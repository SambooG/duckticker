import React from "react";
import "./style.css";
// KYLE: I made this because I thought we'd need it, but maybe we don't?
// I don't know?

const Duck = (props) => {
  return(
  <div className="duck">
      <p>APPL</p>
  </div>
  )
}

export default Duck;
